# Logs

Build and verification logs for local development.

- `make-install.log` - output of `make install` (dependency resolution + TS/labextension build)
- `jest.log` - frontend unit test runs (`make test` / `jlpm test`)
- `ui-tests.log` - Playwright/Galata integration test runs
